# 2026-05-20 22:59 - Phase 14 3D-as-Truth Refactor

- Migrated built-in templates from `isoBoxes` plus per-view SVG arrays to `boxes` only.
- Added shared `core/box-resolver.js` and rewired SVG/iso renderers to resolve the same world-space boxes.
- Added focused unit tests for box rotation, template box resolution, projection, and advisory min/max behavior.
- Synced `alpha-studio/public/interior-design/src` and `skills` from the source engine.
