# 2026-07-02 03:52 - Phase A Hotfix

## Changed

- Fixed `cab-base-rounded-end` by replacing ternary handle positioning with two conditional `"if"` cylinder shapes.
- Added per-shape error isolation in `renderTemplate`, recording skipped shape errors into `_validationWarnings`/`warnings`.
- Added `===` and `!==` tokenizer aliases to the expression engine.
- Updated raw box rendering so `item.color` derives shaded faces instead of using the global wood palette.
- Added focused Node tests for template crash prevention, strict equality aliases, invalid-shape skipping, and raw color faces.
- Synced changed runtime/test files into `alpha-studio/public/interior-design/src`.

## Verified

- `node --check` on edited engine/backend files.
- `node --test tools/interior-design-engine/src/template-engine/__tests__/interpreter.test.mjs tools/interior-design-engine/src/__tests__/box-resolver.test.mjs`
- Public mirror Node tests passed.
- Source/public mirror SHA256 comparison passed for changed runtime files.
- Backend seed script upserted 14 interior templates.
